# Shake&Tune Docker server dependencies
# These run on the server side (x86/arm64) — NOT on the K1C

# Shake&Tune core dependencies
GitPython==3.1.41
matplotlib==3.9.4
numpy==2.2.2
zstandard==0.23.0

# HTTP server
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20
